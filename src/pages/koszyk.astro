---
import BaseLayout from '@layouts/BaseLayout.astro';
import { products } from '@data/products';

const cartCatalog = products.map((product) => ({
  slug: product.slug,
  name: product.name,
  price: product.price,
  priceValue: product.priceValue,
  weight: product.weight,
  image: `/products/${product.slug}.webp`,
  imageAlt: product.imageAlt
}));
const cartCatalogJson = JSON.stringify(cartCatalog).replace(/</g, '\\u003c');
---

<BaseLayout
  title="Koszyk | KIVA Specialty Coffee Roastery"
  description="Frontendowy koszyk KIVA z produktami dodanymi podczas bieżącej sesji przeglądania."
>
  <section class="page-hero cart-hero">
    <p class="eyebrow">Koszyk KIVA</p>
    <h1 class="products-hero-title">Koszyk</h1>
    <p class="lede">Produkty dodane podczas tej sesji. Checkout zostanie dodany później.</p>
  </section>

  <section class="cart-page" data-cart-page aria-live="polite">
    <div class="cart-empty" data-cart-empty hidden>
      <h2>Koszyk jest pusty</h2>
      <p>Dodaj kawę z listy produktów, a pojawi się tutaj do podglądu ilości i sumy.</p>
      <a class="button button-dark" href="/produkty/">Wróć do produktów</a>
    </div>

    <div class="cart-items" data-cart-items></div>

    <div class="cart-summary" data-cart-summary hidden>
      <div class="cart-subtotal-row">
        <span>Suma</span>
        <strong data-cart-subtotal>0 zł</strong>
      </div>
      <p>Checkout zostanie dodany później.</p>
    </div>
  </section>

  <script type="application/json" data-cart-catalog set:html={cartCatalogJson}></script>
</BaseLayout>
